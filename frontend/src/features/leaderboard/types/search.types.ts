export type SearchUser = {
  id: number;
  avatarURL: string;
  name: string;
  visits: number;
  avgRating: number;
};

export type SearchPizzeria = {
  id: number;
  name: string;
  memberNumber: number;
  nation: string;
};
